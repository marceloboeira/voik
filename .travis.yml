language: rust

cache: cargo

os:
  - linux
  - osx

rust:
  - stable
  - beta
  - nightly

script:
  - cargo build --verbose --all
  - cargo test --verbose --all --all-features
  - cd commit_log/
  - cargo test --tests
